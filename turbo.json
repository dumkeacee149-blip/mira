{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "@proj-mira/electron-vueuse#build": {
      "dependsOn": ["@proj-mira/electron-eventa#build"],
      "outputs": ["dist/**"]
    }
  }
}
